let path = '../assets/';

const Icons = {
    logo: require(`${path}logo.png`),
    back: require(`${path}back.png`),
    camera: require(`${path}camera.png`),
    gallery: require(`${path}gallery.jpg`),
};

export default Icons;